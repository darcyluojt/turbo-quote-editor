<div class="container">
  <%= link_to 'back', quotes_path  %>
  <div class="header">
    <h1>New Quote</h1>
  </div>
    <%= turbo_frame_tag @quote do %>
      <%= render "form", quote: @quote %>
    <% end %>
</div>
