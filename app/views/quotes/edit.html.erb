<div class="container">
  <%= link_to 'back', quotes_path  %>
  <div class="header">
    <h1>Edit Quote</h1>
  </div>
  <%= turbo_frame_tag "quote_#{@quote.id}" do %>
    <%= render "form", quote: @quote %>
  <% end %>
</div>
